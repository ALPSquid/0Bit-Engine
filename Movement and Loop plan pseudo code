MovementSystem

checkXCollisions()
checkYCollisions()

if ((not player.onGround) and (player.velocity.y < 9)) {
	player.velocity.y = player.velocity.y - gravity
}

if (jump key pressed) {
	player.velocity.y = player.velocity.y + jumpVelocity
}
